# AssureQai API Gateway - Local Development
# NODE_ENV is intentionally NOT set here — Next.js and NestJS set it automatically
# (next build → production, next dev → development)
PORT=3000
JWT_SECRET=dev-secret-change-in-production-abc123xyz
JWT_EXPIRES_IN=24h
CORS_ORIGINS=http://localhost:3000,http://localhost:4200,http://localhost:3001,http://localhost:3002
NEXT_PUBLIC_API_URL=http://localhost:3000
MONGODB_URI=mongodb+srv://keenkanish:7dmcvwOLRFubnsWS@cluster0.knu2xik.mongodb.net/assureqai?appName=Cluster0
# Portal URLs
NEXT_PUBLIC_LANDING_URL=http://localhost:4200
NEXT_PUBLIC_CLIENT_PORTAL_URL=http://localhost:3001
NEXT_PUBLIC_ADMIN_PORTAL_URL=http://localhost:3002

# Usage Reporting (Instance → Admin Panel)
# ADMIN_PANEL_URL: The URL of the central admin API gateway (where audit reports are sent)
# INSTANCE_API_KEY: The API key for this instance (generated in admin panel → Instances → [instance] → API Key)
ADMIN_PANEL_URL=http://localhost:3000
INSTANCE_API_KEY=
