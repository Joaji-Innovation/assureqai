# AssureQai API Gateway Configuration
# Copy this file to .env.local and fill in the values

# App
NODE_ENV=development
PORT=3000

# JWT - REQUIRED (generate with: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:4200

# Database (MongoDB)
# MONGODB_URI=mongodb://localhost:27017/assureqai

# Redis
# REDIS_HOST=localhost
# REDIS_PORT=6379

# Gemini AI
# GEMINI_API_KEY=your-gemini-api-key

# Usage Tracking & Reporting
# INSTANCE_API_KEY identifies this deployment's billing instance.
# All API calls and audit credits are tracked against it.
# Get the API key from the admin panel after creating the instance.
# ADMIN_PANEL_URL=https://api.assureqai.app
# INSTANCE_API_KEY=aq_your-api-key-from-admin-panel

# Dodo Payments
# DODO_API_KEY=your-dodo-api-key
# DODO_WEBHOOK_SECRET=your-dodo-webhook-secret
# DODO_ENV=test

# ─── Multi-Tenant Settings ───────────────────────────────
# ENABLE_REGISTRATION=false
# Set to 'true' ONLY on the tenant backend (Mode B: shared multi-tenant).
# When false, POST /users/register returns 403.
# Admin backend and isolated instances (Mode A/C) should keep this off.
